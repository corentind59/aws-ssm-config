{
  "name": "@corentind/aws-ssm-config",
  "version": "0.1.2",
  "description": "Fetch your configuration from AWS SSM Parameter store easily",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "private": false,
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "aws-sdk": "^2.641.0",
    "aws-xray-sdk-core": "^2.5.0"
  }
}
